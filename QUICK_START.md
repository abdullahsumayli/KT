# 🚀 دليل البدء السريع - منصة كيتشن تك

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- ✅ Python 3.10+
- ✅ Flutter 3.35.5+
- ✅ VS Code أو أي IDE

---

## ⚡ التشغيل السريع

### 1️⃣ Backend (5 دقائق)

```bash
# 1. افتح Terminal في مجلد Backend
cd d:/KT/backend

# 2. تفعيل البيئة الافتراضية
.venv\Scripts\activate

# 3. تشغيل الخادم
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**✅ جاهز!** الخادم يعمل على: http://localhost:8000

**📚 افتح التوثيق**: http://localhost:8000/docs

---

### 2️⃣ Frontend (3 دقائق)

```bash
# 1. افتح Terminal جديد في مجلد Frontend
cd d:/KT/frontend/kitchentech_app

# 2. شغل التطبيق
flutter run -d chrome
```

**✅ جاهز!** التطبيق يعمل على المتصفح

---

## 🔐 تسجيل الدخول

### حساب المدير (جاهز للاستخدام):

```
Email: admin@kitchentech.sa
Password: admin123456
```

⚠️ **هام**: غيّر كلمة المرور بعد أول تسجيل دخول!

### إنشاء حسابات جديدة:

1. **للعملاء**: اختر "عميل" عند التسجيل
2. **للمعلنين**: اختر "معلن" وأدخل بيانات الشركة

---

## 🧪 اختبار سريع (10 دقائق)

### الخطوة 1: سجل دخول كمدير

```
1. افتح Frontend
2. اذهب لتسجيل الدخول
3. استخدم: admin@kitchentech.sa / admin123456
4. ستظهر لك لوحة المدير
```

### الخطوة 2: اعرض الإحصائيات

```
1. من لوحة المدير، اعرض Dashboard
2. ستجد: عدد المستخدمين، الإعلانات، الاشتراكات، الإيرادات
```

### الخطوة 3: سجل معلن جديد

```
1. سجل خروج
2. سجل حساب جديد
3. اختر "معلن"
4. أدخل:
   - Email: advertiser@test.com
   - Username: advertiser1
   - Password: password123
   - الاسم: معلن تجريبي
   - الشركة: شركة المطابخ الحديثة
   - المدينة: الرياض
```

### الخطوة 4: أنشئ إعلان

```
1. من لوحة التحكم، اضغط "إضافة إعلان جديد"
2. املأ التفاصيل:
   - العنوان: مطبخ حديث للبيع
   - الوصف: مطبخ جديد بحالة ممتازة
   - السعر: 15000
   - المدينة: الرياض
   - النوع: modern
   - المادة: wood
3. احفظ الإعلان
4. الحالة ستكون: "بانتظار المراجعة"
```

### الخطوة 5: راجع الإعلان كمدير

```
1. سجل خروج وسجل دخول كمدير
2. اذهب لـ "إدارة الإعلانات"
3. ستجد الإعلان الجديد
4. اضغط "موافقة"
5. الإعلان أصبح مرئياً للجميع
```

### الخطوة 6: سجل عميل واعرض الإعلان

```
1. سجل خروج
2. سجل حساب عميل جديد:
   - Email: client@test.com
   - Username: client1
   - Password: password123
3. تصفح الإعلانات
4. ستجد الإعلان المعتمد
5. أضفه للمفضلة ❤️
```

---

## 📊 استكشاف API

### افتح Swagger UI:

```
http://localhost:8000/docs
```

### جرب Endpoints:

1. **GET /api/plans** - اعرض الباقات (لا يحتاج authentication)
2. **POST /api/auth/login** - سجل دخول
3. **GET /api/profile/me** - اعرض ملفي (يحتاج token)
4. **GET /api/favorites** - قائمة المفضلة (يحتاج token)

---

## 🗂️ الملفات المهمة

### للقراءة:

1. **d:/KT/PROJECT_SUMMARY.md** - ملخص شامل للمشروع
2. **d:/KT/backend/README.md** - دليل Backend
3. **d:/KT/backend/TESTING.md** - دليل اختبار API

### للتعديل:

1. **d:/KT/frontend/kitchentech_app/lib/config/api_config.dart** - إعدادات API
2. **d:/KT/backend/app/core/config.py** - إعدادات Backend

---

## 🎯 الميزات الجاهزة للاستخدام

### ✅ Frontend (14 شاشة):

- الصفحة الرئيسية
- الإعلانات (عرض، تفاصيل، بحث، فلترة)
- تسجيل الدخول والتسجيل
- لوحة التحكم للمعلنين
- إنشاء إعلان (Wizard)
- الباقات والاشتراكات
- صفحة الدفع
- المفضلة
- الملف الشخصي
- كيف يعمل
- التواصل
- لوحة المدير (كاملة)

### ✅ Backend (10 API Modules):

- Authentication (تسجيل، دخول، معلومات المستخدم)
- Listings (CRUD كامل مع صور)
- Plans & Subscriptions (باقات واشتراكات)
- Profile (إدارة الملف الشخصي)
- Favorites (قائمة المفضلة)
- Contact (رسائل التواصل)
- Settings (إعدادات الموقع)
- Admin Panel (لوحة تحكم شاملة)

### ✅ Database (8 جداول):

- users (المستخدمين)
- listings (الإعلانات)
- listing_images (الصور)
- plans (الباقات)
- subscriptions (الاشتراكات)
- favorites (المفضلة)
- contact_messages (رسائل التواصل)
- site_settings (الإعدادات)

---

## ❓ الأسئلة الشائعة

### س: كيف أغير رقم المنفذ للـ Backend؟

```bash
uvicorn app.main:app --reload --port 8080
```

ولا تنسى تحديث `api_config.dart` في Frontend.

### س: كيف أستخدم PostgreSQL بدلاً من SQLite؟

في ملف `.env`:

```
DATABASE_URL=postgresql://user:pass@localhost/kitchentech
```

### س: كيف أرفع صور الإعلانات؟

استخدم endpoint:

```
POST /api/images/upload
Content-Type: multipart/form-data
```

### س: كيف أختبر من جهاز موبايل؟

في `api_config.dart` غير:

```dart
static const String baseUrl = 'http://YOUR_IP:8000';
```

### س: كيف أحذف قاعدة البيانات وأبدأ من جديد؟

```bash
cd d:/KT/backend
rm kitchentech.db
python create_tables.py
python init_default_data.py
```

---

## 🔧 استكشاف الأخطاء

### مشكلة: Backend لا يعمل

**الحل**:

```bash
# تأكد من تفعيل البيئة الافتراضية
.venv\Scripts\activate

# تأكد من تثبيت المكتبات
pip install -r requirements.txt

# شغل الخادم
uvicorn app.main:app --reload
```

### مشكلة: Frontend لا يتصل بـ Backend

**الحل**:

1. تأكد من أن Backend يعمل (http://localhost:8000/docs)
2. تحقق من `api_config.dart` - baseUrl صحيح؟
3. تحقق من CORS في Backend

### مشكلة: خطأ في تسجيل الدخول

**الحل**:

1. تأكد من صحة Email/Password
2. تحقق من أن الحساب موجود في قاعدة البيانات
3. راجع logs في Backend terminal

---

## 📞 الحصول على مساعدة

### إذا واجهت مشكلة:

1. **راجع الـ Logs**: تحقق من Terminal الخاص بـ Backend
2. **راجع API Docs**: http://localhost:8000/docs
3. **راجع Database**: استخدم `sqlite3 kitchentech.db`
4. **راجع الملفات**: اقرأ README.md و TESTING.md

---

## 🎓 ملاحظات مهمة

### الأمان:

- ⚠️ غيّر كلمة مرور المدير الافتراضية
- ⚠️ لا تشارك JWT tokens
- ⚠️ استخدم HTTPS في الإنتاج
- ⚠️ احفظ SECRET_KEY في متغيرات البيئة

### الأداء:

- 💡 استخدم PostgreSQL في الإنتاج
- 💡 فعّل caching للإعلانات
- 💡 ضغط الصور قبل الرفع
- 💡 استخدم CDN للملفات الثابتة

### الجودة:

- ✅ جميع API endpoints لها توثيق
- ✅ جميع Endpoints لها validation
- ✅ Errors لها رسائل واضحة
- ✅ جميع Screens responsive

---

## 🚀 الخطوة التالية

بعد التأكد من أن كل شيء يعمل:

### 1. Customization (التخصيص)

- غيّر الألوان في Frontend
- عدّل الشعار
- أضف نصوص الشروط والأحكام
- خصص رسائل البريد الإلكتروني

### 2. Enhancement (التحسين)

- أضف نظام الدفع (Stripe/PayTabs)
- أضف إشعارات Push
- أضف Chat بين المستخدمين
- أضف تقييمات ومراجعات

### 3. Deployment (النشر)

- احصل على domain
- انشر Backend على Railway/Heroku
- انشر Frontend على Firebase/Netlify
- اربط database حقيقية

---

## 🎉 تهانينا!

لديك الآن منصة كاملة وجاهزة للاستخدام! 🎊

**ابدأ بتجربة المنصة واستكشف جميع الميزات.**

---

**بناها**: GitHub Copilot + Claude Sonnet 4.5  
**التاريخ**: ديسمبر 2025  
**الإصدار**: 1.0.0

**استمتع بتطوير منصتك! 💪**
